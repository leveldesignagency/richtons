# Home page — paragraph line heights (reference)

Central reference for body copy **line-height** on the main landing sections. Values are unitless ratios (multiply by font size).

| Location | CSS module class | Line height |
|----------|------------------|-------------|
| Everything you need to know — lead (“At Richtons, we understand…”) | `.needToKnowLead` (`page.module.css`) | **1.46** |
| Meet the team — intro paragraph | `.teamSectionIntro` | **1.48** |
| Core values — intro paragraph | `.valuesIntro` | **1.50** |

Progressive title timing presets live on `RevealWordsOnScroll` (`revealTiming`: `default` | `fast` | `late`) in `src/components/RevealWordsOnScroll.tsx`.
